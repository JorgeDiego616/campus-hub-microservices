import { DocumentNode, GraphQLSchema } from 'graphql';
import { GraphQLSchemaModule } from './resolvers';
export type BuildSubgraphSchemaInput = (GraphQLSchemaModule | DocumentNode)[] | DocumentNode;
/**
 * Builds an executable subgraph schema from federation SDL.
 *
 * The schema gains whatever the document `@link`s from the federation
 * specification, plus the `_service` and `_entities` root fields the router uses
 * to introspect and resolve entities.
 */
export declare function buildSubgraphSchema(modulesOrSDL: BuildSubgraphSchemaInput): GraphQLSchema;
//# sourceMappingURL=buildSubgraphSchema.d.ts.map